# 引き継ぎ書 — セミナー録画から議事録を作る仕組み

作成: 2026-08-27 / クラウドで動いていたセッションから、Macのローカルセッションへ引き継ぐためのメモ。

**新しいセッションの Claude へ: まずこのファイルを最後まで読んでから話し始めてください。**
持ち主はエンジニアではありません。話し方のルールは `CLAUDE.md` にあります。そちらも必ず読むこと。

---

## 1. 何をしようとしているか

持ち主が Zoom でセミナー・勉強会をやる。その録画から、

- 文字起こし
- 議事録
- タイムスタンプ付き要約（0:00 開始前 / 1:56 あいさつ… の形）
- ネクストアクション
- 参加者に配るメールの文面

を作り、さらに **あとから「あの話どこだっけ」と質問できる** 状態にしたい。

きっかけは、他の人が送ってきたセミナー案内メールを見て「こういうのを自分でもやりたい」と
思ったこと。NotebookLM に YouTube の URL を入れたが読み込めず、困っていた。

### NotebookLM が読めなかった理由（調査済み・確定）

NotebookLM の YouTube ソースは **一般公開＋字幕あり＋投稿から72時間経過** の動画しか
受け付けない。しかも **NotebookLM は動画を文字起こししていない** — YouTube が持っている
字幕データを取り込んでいるだけ。だから字幕が無い、または限定公開の録画は弾かれる。
セミナー録画はほぼ全部これに当たる。

回避策は「文字起こしをテキストとして渡す」の一択。それを自動化したのが今回の成果物。

---

## 2. 作ったもの（すべてコミット済み）

| ファイル | 何をするもの |
|---|---|
| `seminar_notes.py` | 本体。文字起こし取得 → 議事録作成 → 質問応答 |
| `.claude/skills/seminar-notes/SKILL.md` | 話しかけるだけで上記が動くようにするスキル |
| `SEMINAR_NOTES.md` | 使い方・つまずきどころの手順書 |
| `CLAUDE.md` | **話し方のルール。** 専門用語で置いていかないこと |

### `seminar_notes.py` の中身

文字起こしの取り方を上から順に試す。

0. **手元の文字起こしをそのまま読む** — `.txt` `.vtt` `.srt` `.json3` を渡した場合
1. **YouTube の字幕を API で取得** — 限定公開でも字幕があれば通る。鍵不要
2. **yt-dlp で自動生成字幕を取得** — 1が塞がれたときの予備。鍵不要
3. **音声をダウンロードして Gemini で文字起こし** — 字幕が無い動画・ローカル録画用

3 だけ `GEMINI_API_KEY` が要る（予備で `OPENAI_API_KEY`）。**最初から要求しないこと。**
実際に必要になった場面で、理由とセットで頼む。

議事録の作成と質問応答は Claude が担当。`claude` CLI（サブスク枠）を優先し、
無ければ `ANTHROPIC_API_KEY` の従量課金にフォールバックする（`curate.py` と同じ2段構え）。

### 主なコマンド

```bash
python3 seminar_notes.py "<URL またはファイル>" --title "<セミナー名>"
python3 seminar_notes.py "<URL>" --title "..." --focus "ツールの使い方を手順まで詳しく"
python3 seminar_notes.py --ask "<質問>"          # 直近の回に質問
python3 seminar_notes.py --list                  # 保存済み一覧
```

出力は `seminars/<スラッグ>/` に `transcript.txt` / `notes.md` / `meta.json`。
中身は社外に出せないことが多いので `seminars/` は `.gitignore` 済み。

**`python` ではなく `python3`。** 最近の macOS には `python` が無い。

---

## 3. 動作確認できていること・できていないこと

| 項目 | 状態 |
|---|---|
| 議事録・要約・ネクストアクションの生成 | ✅ 実際に Claude を呼んで確認済み |
| 「ツールの使い方」セクション（番号付き手順＋タイムスタンプ） | ✅ 確認済み |
| `--ask` での質問応答（回答に時刻が付く） | ✅ 確認済み |
| 字幕ファイル（VTT / SRT / json3）の読み込み | ✅ 確認済み |
| **YouTube から実際に字幕・音声を取る経路** | ❌ **未確認** |

最後の1つが未確認なのが、ローカルに移る理由。**新しいセッションで最初にやるのはここ。**

---

## 4. なぜローカルに移るのか

前のセッションは **デスクトップアプリから始めたが、Anthropic のクラウドで動いていた**
（`origin: desktop_app` / `environment_kind: anthropic_cloud` を確認済み）。
そのクラウド環境はネットワークポリシーで YouTube への接続を遮断している。

```
www.youtube.com:443 -> gateway answered 403 to CONNECT (policy denial)
```

字幕API・yt-dlp・音声ダウンロード・WebFetch、**4経路すべてが同じ403で失敗**した。
持ち主の設定ミスではないし、動画が限定公開だからでもない。環境側の制約。

Mac のローカルセッションなら YouTube に届くので、そこで初めて通しの確認ができる。

**注意: これは「どのアプリを使うか」の話ではない。** デスクトップアプリでも VS Code 拡張でも、
セッションを始めるときの Environment で **Local** を選べばMacで動く。Cloud を選べばクラウドで動く。
前のセッションではここを取り違えて「ブラウザ版だ」と決めつけ、持ち主を混乱させた。

---

## 5. 新しいセッションで最初にやること

### (1) 状況を確認する

```bash
git status
git branch -a
```

作業は **`claude/notebooklm-youtube-support-3q0s23`** というブランチ（作業用の別バージョン）に
入っている。ローカルのクローンが `main` を見ている場合、上記のファイルはまだ無い。その場合は

```bash
git fetch origin claude/notebooklm-youtube-support-3q0s23
git checkout claude/notebooklm-youtube-support-3q0s23
```

**手順を持ち主に打たせないこと。こちらで実行する。**

### (2) 準備する

```bash
python3 -c "import youtube_transcript_api, yt_dlp, anthropic"   # 足りないものを確認
python3 -m pip install -r requirements.txt                      # 足りなければ入れる
```

### (3) 本命の確認 — 実際の録画で通す

持ち主が最初に依頼したのはこの録画:

```
https://youtu.be/CsT4Yvs7w5Q
```

これはラクムビ（Premiere Pro を操作する動画編集ツール）の勉強会の録画で、
**限定公開**と思われる。持ち主の要望は **「ツールの使い方が詳しく説明されているので、
そこを重点的に」**。

```bash
python3 seminar_notes.py "https://youtu.be/CsT4Yvs7w5Q" \
  --title "ラクムビ勉強会" \
  --focus "ツールの使い方の説明が中心の回です。操作手順を重点的に。"
```

字幕が取れない場合は `--cookies-from-browser chrome`（使っているブラウザ名を確認して）、
それでも駄目なら `GEMINI_API_KEY` を用意してもらって音声から。

**結果はファイルの場所を伝えて終わりにせず、notes.md を読んで会話の中に出すこと。**
持ち主はターミナルを開きたくない。それがこのスキルの存在理由。

---

## 6. まだ決まっていないこと（持ち主の判断が要る）

### (a) 完成版（main）に合流させるか

議事録ツールは今、作業用ブランチにしかない。`main`（完成版）に入れれば、
普通にフォルダを開くだけで使える状態になる。**持ち主の「いいよ」が出るまで実行しないこと。**

### (b) 意図の確認 — これがまだ聞けていない

前のセッションで質問しようとしたが、聞き方が的外れで持ち主に閉じられた。
**同じ聞き方を繰り返さないこと。** まず作って見せてから、実物を前に確認するほうがよい。

聞けていないのは主にこの4点:

1. 録画は普段どこにあるか（Zoomのクラウド録画 / YouTubeに限定公開 / Macにファイル）
2. できた議事録を主に何に使うか（参加者に配る / 自分用の記録 / 講座の教材 / チーム共有）
3. 持ち主は主催する側か、参加する側か
4. 使うのは持ち主だけか、講師チームにも配るか

**特に 1 が入り口を決める。** Zoom のクラウド録画には Zoom 自身の文字起こしがあるので、
YouTube を経由せず直接取れれば一番手数が少ない。今の実装はまだ Zoom に対応していない。
**次に作るならここ。**

---

## 7. 前のセッションでの失敗（繰り返さないこと）

持ち主から直接指摘を受けた点。

1. **専門用語を説明なしに使った。**「このリポジトリで」を何度も言い、
   「リポジトリって何？」と聞かれた。→ `CLAUDE.md` にルールを明文化した
2. **持ち主に手を動かさせようとした。**「これを実行してください」と手順を渡していた。
   持ち主の言葉: 「得意な君が全部やってもらって、僕は判断するっていう仕事だけで良いはず」
   → **こちらで実行できることは実行する**
3. **事実を確認せず決めつけた。** 「ブラウザ版を使っている」と勝手に判断したが、
   実際はデスクトップアプリだった。→ 断定する前に `get_session` 等で確かめる
4. **「同じです」と言って矛盾した説明をした。** 何と何を比べているのかを明示しなかった
5. **筋の違う方向に走った。** 他人が作った議事録を Google ドライブから取ってきてしまった。
   持ち主が求めていたのは「自分でできるようになること」

---

## 8. 用語（持ち主に説明するときの言い換え）

`CLAUDE.md` にも表がある。よく出るもの:

- **リポジトリ** → プロジェクト一式が入ったフォルダ
- **ブランチ** → 同じフォルダの作業用の別バージョン。`main` が完成版
- **マージ** → 作業用バージョンを完成版に合流させること
- **コミット** → 変更に名前を付けて記録する、作業のセーブポイント

---

## 9. 関連ファイル

- `CLAUDE.md` — 話し方のルールと用語表（**最優先で読む**）
- `SEMINAR_NOTES.md` — 使い方・つまずきどころ
- `.claude/skills/seminar-notes/SKILL.md` — スキル本体
- `PROJECT_LOG.md` — プロジェクト全体の経緯
