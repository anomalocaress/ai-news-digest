# このリポジトリで作業するときに、最初に読むこと

## プロジェクト

**世界一わかりやすいAIニュース** — AIニュースを毎朝6時に自動収集し、日本語で要約して
サイト・ポッドキャスト・メールで配信する。専門用語にすべて解説が付くのが差別化。
経緯・決定事項・ハマった罠は **[PROJECT_LOG.md](PROJECT_LOG.md)** に全部ある。

## セッションの最初にやること

**[knowledge/INDEX.md](knowledge/INDEX.md) を読む。**

ここには、運営者がこれまでに集めた「今後の判断に効く知見」が入っている。
イケハヤさん・テツメモさんのニュースレターから毎朝自動で蓄積され、
会話の中で「これ覚えといて」と言われたものもここに入る。

助言や設計をするときは、必ずこの索引を踏まえること。索引に無い話題でも
`python knowledge.py search <語>` で拾えることがある。

### なぜこれが要るのか

Claude はセッションが終われば会話の内容を忘れる。教材を読ませても、
ニュースレターを共有しても、次のセッションには何も残らない。
**リポジトリのファイルに書いたものだけが残る。** それがこの仕組みの理由。

### 知見の扱い方

各項目には確からしさが付いている。ここを混同しないこと。

| 表示 | 意味 | 扱い |
|---|---|---|
| 検証済み | 公式仕様・一次情報 | 事実として使ってよい |
| 出典の主張 | ニュースレターがそう言っている（未検証） | 「〜と言われている」と断って使う |
| 意見・体験 | 個人の体験談 | 参考。一般則として扱わない |

ニュースレターの多くは「出典の主張」。**断言しないこと。**

## 新しい知見を聞いたとき

運営者が「これ覚えといて」「この記事の内容を残しておいて」と言ったら、
その場で記録する。会話に残しただけでは消える。

```bash
python knowledge.py add --title "..." --summary "..." \
  --takeaway "..." --apply "..." --tag 自動化 \
  --source テツメモ --source-type newsletter --confidence claim
```

複数件なら `--json -` に JSON の配列を流し込む。
記録したら `python knowledge.py build` で索引を更新する（`add` は自動で走る）。

**入れる基準**: 今後の判断に効くものだけ。その日限りのニュースは入れない
（それは日次ダイジェストの仕事）。

**書き方**: 出典の本文をそのまま貼らない。**必ず自分の言葉に落とす。**
このリポジトリは公開されているので、他人の有料コンテンツの本文を置いてはいけない。
取り込んだメール本文（`knowledge/inbox/`）は .gitignore で除外済み。

## よく使うコマンド

```bash
python generate_news.py              # ダイジェスト生成（全工程）
python seo_builder.py                # サイト再構築
python knowledge_ingest.py run       # ニュースレター取り込み→蒸留→索引更新
python knowledge.py search <語>      # 蓄積から探す
python knowledge.py report --since 7 # 直近の報告
python knowledge.py stats            # 何がどれだけ溜まっているか
```

## この人の書き方の好み

- 文体は敬体（です・ます）。常体と混ぜない
- 専門用語は必ず開く。中学英語の範囲で読めることが基準
- カタカナ語を不自然に分かち書きしない
- 失敗談と数字を入れる（→ WRITING.md）
