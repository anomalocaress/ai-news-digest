# Claudeに覚えておいてもらう方法

## まず、いちばん大事なこと

**Claudeはセッションが終わると忘れます。**

教材を読ませても、ニュースレターを共有しても、
「これ重要だから覚えといて」と言っても、次のセッションには何も残っていません。
今のセッションでうまく助言が返ってくるのは、**同じ会話の中にいる間だけ**です。

残る場所はひとつだけ。**このリポジトリのファイル**です。
だから知見をファイルに書いて、毎回のセッションで自動的に読ませる仕組みを作りました。

```
ニュースレター（Gmail）─┐
                        ├→ knowledge/knowledge.json → knowledge/INDEX.md
Claudeとの会話 ─────────┘                                    ↓
                                            CLAUDE.md が毎セッション読ませる
```

---

## 方法1：Claudeに話す（推奨・所要1分）

チャットで言うだけです。

> 「テツメモさんの今週のやつ、Skillは教材本体を書き換えずに別に作るって話が良かった。覚えといて」

これで Claude が要点を整理して `knowledge.json` に記録し、索引を更新します。
分類やタグを自分で決める必要はありません。

**記録されたか確認したいとき:**

> 「今なに覚えてる？」「ナレッジの報告して」

---

## 方法2：自動で溜まるのを待つ（何もしない）

毎朝6時の自動実行のときに、Gmailに届いたニュースレターを読んで、
使えそうな知見だけを勝手に記録します。**あなたは何もしなくて構いません。**

見ている差出人:

| 名前 | アドレス |
|---|---|
| イケハヤ | `info@mag.ikehaya.com` / `info@ikemag.jp` |
| テツメモ | `tetumemo@m-newsletter.com` |

増やしたいときは `monetize_config.json` の `knowledge.watch` に足すか、
Claude に「〇〇さんのニュースレターも見て」と言ってください。

宣伝だけのメールは「知見なし」として記録され、二度読み返しません。

---

## 方法3：コマンドで入れる

```bash
python knowledge.py add \
  --title "広告は総額でなく獲得単価で見る" \
  --summary "..." \
  --takeaway "..." \
  --apply "..." \
  --tag 収益化 --source イケハヤ --confidence claim
```

---

## 報告を受け取る

```bash
python knowledge.py report --since 7      # 直近7日ぶんを表示
python knowledge.py report --since 7 --email   # メールで受け取る
python knowledge.py stats                 # 何がどれだけ溜まっているか
```

毎朝の自動実行では GitHub Actions の実行画面に報告が出ます。

---

## 確からしさの3段階

ニュースレターの内容を**事実として鵜呑みにしない**ための区別です。

| 表示 | 意味 |
|---|---|
| 検証済み | 公式仕様や一次情報。事実として使える |
| 出典の主張 | その人がそう言っている。未検証 |
| 意見・体験 | 個人の体験談。一般則ではない |

Claude はこの区別を守って助言します。「出典の主張」は断言しません。

---

## 公開リポジトリなので、ここだけ注意

このリポジトリは誰でも見られます。

- **有料教材やニュースレターの本文をそのまま置かない。** 記録されるのは要約だけです
- 取り込んだメール本文は `knowledge/inbox/` に一時保存され、
  蒸留が終わると削除されます。`.gitignore` 済みなのでコミットもされません
- ナレッジはサイトには一切出ません（`visibility: private`）。生成処理は `knowledge/` を見ません

「これは公開したくない」というものがあれば、Claude に言えば消します。

---

## 中身を直接見たいとき

| ファイル | 中身 |
|---|---|
| `knowledge/INDEX.md` | 索引。Claude が毎回読むのはここ |
| `knowledge/knowledge.json` | 全データ |
| `knowledge/inbox/` | 取り込んだメール本文（蒸留待ち・コミットされない） |
