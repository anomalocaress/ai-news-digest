# ニュースレター（Kit）のはじめかた

配信サービスは **Kit**（旧ConvertKit / kit.com）。
無料で読者1万人まで、RSSからの自動配信に対応しています。
**こちら側の実装は済んでいる**ので、あなたの作業は以下の3ステップだけです。

## ステップ1：Kitのアカウントを作る（5分）

1. https://kit.com で無料アカウントを作成（メールアドレスだけで始められます）
2. 初期設定で聞かれる「何を作るか」は Newsletter を選べばOK

## ステップ2：RSS自動配信を設定する（5分）

Kitの管理画面で RSS 連携（Automate → RSS など、メニュー名は変わることがあります）を開き、
このURLを登録します：

```
https://teraco-labo.github.io/ai-news-digest/feed.xml
```

- 配信頻度は「新着が出たら自動送信（daily）」を選ぶ
- 本文は **RSSのコンテンツをそのまま使う** 設定にする
  （フィードには、3行まとめ・音声リンク・記事カード・今日の用語まで
  整形済みのHTMLが丸ごと入っています。Kit側で件名だけ整えれば完成です）
- 件名のおすすめ: `【{日付}】今日のAIニュース ─ 3分でわかる`

これで**毎朝6時すぎに、登録読者へ自動でメールが届く**ようになります。
あなたが毎日やることはありません。

## ステップ3：登録フォームのURLをサイトに繋ぐ（5分）

1. Kitで登録用のランディングページかフォームを1つ作る（テンプレを選ぶだけ）
2. そのURLをコピーして、Claudeに「KitのURLはこれ → https://...」と伝える
   （または `monetize_config.json` の `newsletter.signup_url` に貼る）

これでサイト全ページに「✉️ メールで受け取る」ボタンが現れます。

### 発展：フォームをサイトに直接埋め込む（あとででOK）

Kitのフォームには「埋め込みコード（HTML）」があります。
それをClaudeに渡せば、リンクで飛ばす代わりに**サイト内で直接メールアドレスを
入力できる形**にできます（登録率が上がります）。設定は `newsletter.embed_html`。

## 法律まわり（Kitが自動でやってくれること）

特定電子メール法が求める「配信解除リンク」「送信者情報」はKitが全メールに自動で入れます。
初期設定で住所（事業所の所在地）を求められるのはこのためです。

## うまくいったかの確認

自分のメールアドレスで登録 → 翌朝6時すぎにメールが届けば成功。
届いたメールの末尾に「📘 今日の用語」が付いているのが本物の証拠です。
